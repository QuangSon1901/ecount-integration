# GLOBAL RULES

1. NEVER break existing system
2. NEVER modify `orders` logic blindly
3. ALWAYS isolate OMS logic
4. ALWAYS create new migrations (no edit old)
5. ALWAYS consider cron side effects
6. USE job queue for async tasks

7. Code must be:
   - Simple
   - Clear
   - Production-safe

8. If unclear → ASK

9. Do NOT jump phases